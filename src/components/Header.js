import React from 'react';
import Link from './Link';

const Header = () => {
  return (
    <header className="App-header">
      <div className="logo">React Widgets</div>
    </header>
  );
};

export default Header;
